name: Deploy Pages with UX Fix

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  pages: write
  id-token: write

jobs:
  deploy:
    runs-on: ubuntu-latest

    environment:
      name: github-pages

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: false

      - name: Prepare site
        run: |
          mkdir -p _site
          cp -r . _site

      - name: UX post-process
        run: |
          cd _site

          # 1. Insert NAV if not present

